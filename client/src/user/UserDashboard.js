import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
  return (
    <Base
      title="User Dashboard"
      description="A place to manage orders and account"
    ></Base>
  );
};

export default UserDashboard;
